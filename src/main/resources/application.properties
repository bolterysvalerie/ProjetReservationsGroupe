spring.application.name=Reservations

# Connect to DataBase MySQL
#spring.datasource.url=jdbc:mysql://localhost:3306/groupe_reservations?autoreconnect=true&useSSL=false&serverTimezone=Europe/Brussels&useLegacyDatetimeCode=false
#spring.datasource.username=root
#spring.datasource.password=

# 1. URL JDBC construite à partir des variables CLEVER_MYSQL_*
spring.datasource.url=jdbc:mysql://${bh6rbqxol4u0eo3fm1lr-mysql.services.clever-cloud.com}:${3306}/${bh6rbqxol4u0eo3fm1lr}?useSSL=false&serverTimezone=UTC

# 2. Identifiants extraits des variables CLEVER_MYSQL_*
spring.datasource.username=${u0mbg0tjzl0xmbry"}
spring.datasource.password=${qNHNMPHJrsOqCsrxo5Z0}

# 3. (Optionnel) Afficher les requêtes SQL dans les logs
#spring.jpa.show-sql=true

# 4. (Optionnel) Choix du comportement DDL au démarrage
#    update pour créer/mettre à jour les tables selon vos entités
#spring.jpa.hibernate.ddl-auto=update

# 5. (Optionnel) Dialecte si vous avez besoin de forcer MySQL 8+
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

##spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Migration by flyway
spring.flyway.enabled=true
spring.flyway.debug=true
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Enable use of hidden input to override HTML method in form (put & delete)
spring.mvc.hiddenmethod.filter.enabled=true

# Stripe api
stripe.public.key = ${PUBLIC_KEY}
stripe.secret.key = ${SECRET_KEY}